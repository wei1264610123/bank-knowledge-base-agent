# 银行问答系统 - 项目指令（AGENTS.md）

本文件由 OpenCode 自动加载注入所有会话，用于规范自动化工作流。

## 代码改动后自动审查（重要）

**每次完成代码改动**（编辑 / 新建 / 删除文件，包括前端与后端）并准备在回复中汇报时，**自动调用 `reviewer` subagent** 审查本次改动：

- 在回复前启动：`用 reviewer subagent 审查我刚完成的代码改动`
- 等待其审查结果，并在最终回复中简要汇报：问题总数 + 最严重的 1~3 条问题（含文件路径）。

## 测试约定

- 涉及**后端**改动或用户要求"跑测试 / 测试报告"时，自动调用 `tester` subagent（已绑定 `unit-test` 技能）执行单元测试并产出 HTML 报告。
- 测试必须使用独立测试库，禁止真实调用阿里云百炼 API。

## 项目运行约定

- 启动后端：在 `backend/` 目录运行 `python run.py`（Python 完整路径 `C:\Users\13445\miniconda3\python.exe`）。
- **改动后端代码后必须重启后端**验证回归。
- 前端为 Vite dev 模式（node 在 `C:\node-v20.18.1-win-x64`，不在 PATH）。