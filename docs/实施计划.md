# 银行知识库智能问答系统 - 实施计划

## 技术选型确认

| 组件 | 选择 | 说明 |
|------|------|------|
| **LLM** | 阿里云百炼（通义千问 qwen-plus） | 免费额度，中文优化 |
| **Embedding** | OpenAI text-embedding-3-small | 检索效果好 |
| **后端** | FastAPI + Python 3.11 | 异步高性能 |
| **AI框架** | LangChain | RAG核心框架 |
| **主数据库** | SQLite（开发）/ PostgreSQL（生产） | 本地部署简化 |
| **向量库** | Milvus Lite（本地） | 无需Docker，嵌入式 |
| **缓存** | 本地文件缓存 / Redis（可选） | 本地部署 |
| **前端** | Vue 3 + Element Plus + Vite | 企业级UI |
| **部署** | 本地直接运行，不使用Docker | 简化部署 |

---

## 开发阶段详细计划

### 第一阶段：项目初始化与基础框架（第1周）

#### 1.1 后端项目初始化
```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py              # FastAPI入口
│   ├── config.py            # 配置管理
│   ├── database.py          # 数据库连接
│   ├── models/              # SQLAlchemy模型
│   ├── schemas/             # Pydantic请求/响应模型
│   ├── api/                 # API路由
│   ├── services/            # 业务逻辑
│   ├── core/                # 核心功能（安全、依赖注入）
│   └── rag/                 # RAG相关
├── requirements.txt
├── .env.example
└── run.py                   # 启动脚本
```

**任务清单**：
- [ ] 创建FastAPI项目，配置CORS、异常处理
- [ ] 配置SQLAlchemy异步连接（SQLite/PostgreSQL）
- [ ] 配置日志系统
- [ ] 创建基础目录结构

#### 1.2 数据库设计与迁移
- [ ] 创建users表（用户认证）
- [ ] 创建chat_sessions表（会话管理）
- [ ] 创建chat_messages表（对话记录）
- [ ] 创建documents表（文档管理）
- [ ] 创建document_chunks表（文档分块）
- [ ] 创建categories表（分类管理）
- [ ] 配置Alembic迁移工具

#### 1.3 用户认证模块
- [ ] JWT Token生成与验证
- [ ] 密码加密（bcrypt）
- [ ] 注册API（POST /api/auth/register）
- [ ] 登录API（POST /api/auth/login）
- [ ] 获取当前用户（GET /api/auth/me）
- [ ] 修改密码（PUT /api/auth/password）
- [ ] 默认管理员账号初始化（admin/123456）

---

### 第二阶段：知识库管理模块（第2周）

#### 2.1 Milvus Lite集成
```python
# 使用Milvus Lite，无需启动服务
from pymilvus import MilvusClient

client = MilvusClient("milvus_demo.db")  # 本地文件存储
```

- [ ] 配置Milvus Lite连接
- [ ] 创建向量集合（collection）
- [ ] 配置索引参数

#### 2.2 文档处理流水线
```python
# LangChain文档处理链
from langchain_community.document_loaders import PyPDFLoader, Docx2txtLoader, TextLoader
from langchain.text_splitter import RecursiveCharacterTextSplitter
from langchain_openai import OpenAIEmbeddings
from langchain_milvus import Milvus
```

- [ ] PDF文档加载器
- [ ] Word文档加载器
- [ ] TXT/Markdown加载器
- [ ] 智能文本分割（chunk_size=500, overlap=100）
- [ ] OpenAI Embedding向量化
- [ ] 存储到Milvus

#### 2.3 知识库管理API
```
POST   /api/knowledge/upload          # 上传文档
GET    /api/knowledge/documents       # 文档列表（分页）
GET    /api/knowledge/documents/{id}  # 文档详情
DELETE /api/knowledge/documents/{id}  # 删除文档
POST   /api/knowledge/documents/{id}/reprocess  # 重新处理
GET    /api/knowledge/categories      # 分类列表
POST   /api/knowledge/categories      # 创建分类
```

- [ ] 实现所有知识库管理API
- [ ] 管理员权限验证中间件
- [ ] 文件上传大小限制（10MB）
- [ ] 支持的文件类型验证

---

### 第三阶段：RAG智能问答核心（第3-4周）

#### 3.1 LangChain RAG Chain配置
```python
from langchain_openai import ChatOpenAI, OpenAIEmbeddings
from langchain_milvus import Milvus
from langchain.chains import create_retrieval_chain
from langchain.chains.combine_documents import create_stuff_documents_chain
from langchain.prompts import ChatPromptTemplate

# LLM配置（阿里云百炼）
llm = ChatOpenAI(
    model="qwen-plus",
    base_url="https://dashscope.aliyuncs.com/compatible-mode/v1",
    api_key="your-dashscope-api-key",
    temperature=0.7,
    streaming=True  # 启用流式响应
)

# Embedding配置
embeddings = OpenAIEmbeddings(model="text-embedding-3-small")

# 向量存储
vector_store = Milvus(
    embedding_function=embeddings,
    collection_name="bank_kb",
    connection_args={"host": "localhost", "port": 19530}
)

# 检索器
retriever = vector_store.as_retriever(
    search_type="similarity",
    search_kwargs={"k": 5}  # 返回最相似的5个片段
)
```

#### 3.2 Prompt模板设计
```python
SYSTEM_PROMPT = """你是一个专业的银行客服助手，名叫"小银"。

## 角色定位
- 你是银行的智能客服，专门回答用户关于银行业务的问题
- 你的回答必须基于提供的知识库内容，确保准确性和专业性
- 如果知识库中没有相关信息，请诚实地告知用户，并建议联系人工客服

## 回答规范
1. 回答要准确、专业、简洁、友好
2. 使用中文回答
3. 在回答中标注引用来源，格式：[来源: 文档名称]
4. 如果问题不清晰，请礼貌地要求用户澄清
5. 涉及敏感操作（如转账、密码修改）时，提醒用户注意安全

## 知识库内容
{context}

## 历史对话
{chat_history}
"""
```

#### 3.3 流式响应实现（SSE）
```python
from fastapi.responses import StreamingResponse

async def stream_chat(session_id: str, message: str):
    """流式问答"""
    # 1. 检索相关文档
    docs = retriever.invoke(message)
    
    # 2. 构建响应流
    async def event_generator():
        async for chunk in llm.astream(prompt):
            yield f"data: {json.dumps({'content': chunk})}\n\n"
        yield "data: [DONE]\n\n"
    
    return StreamingResponse(event_generator(), media_type="text/event-stream")
```

#### 3.4 引用来源提取
```python
def extract_references(docs):
    """提取引用来源"""
    references = []
    seen = set()
    for doc in docs:
        source = doc.metadata.get("source", "未知")
        if source not in seen:
            references.append({
                "content": doc.page_content[:300],
                "source": source,
                "page": doc.metadata.get("page", "N/A")
            })
            seen.add(source)
    return references
```

#### 3.5 会话管理
```
POST   /api/chat/sessions              # 创建新会话
GET    /api/chat/sessions              # 会话列表
GET    /api/chat/sessions/{id}         # 会话详情
DELETE /api/chat/sessions/{id}         # 删除会话
PUT    /api/chat/sessions/{id}/title   # 修改会话标题
POST   /api/chat/completions           # 发送消息（流式）
GET    /api/chat/sessions/{id}/messages # 消息历史
```

---

### 第四阶段：前端开发（第5-6周）

#### 4.1 前端项目结构
```
frontend/
├── src/
│   ├── api/                 # API请求封装
│   │   ├── auth.ts
│   │   ├── chat.ts
│   │   ├── knowledge.ts
│   │   └── request.ts       # Axios封装
│   ├── components/          # 通用组件
│   │   ├── ChatMessage.vue  # 消息气泡
│   │   ├── ReferenceCard.vue # 引用卡片
│   │   └── FileUpload.vue   # 文件上传
│   ├── views/               # 页面
│   │   ├── Login.vue        # 登录页
│   │   ├── Register.vue     # 注册页
│   │   ├── Chat.vue         # 问答页
│   │   ├── Knowledge.vue    # 知识库管理（管理员）
│   │   └── Profile.vue      # 个人中心
│   ├── stores/              # Pinia状态管理
│   │   ├── auth.ts
│   │   ├── chat.ts
│   │   └── knowledge.ts
│   ├── router/              # 路由配置
│   ├── utils/               # 工具函数
│   ├── types/               # TypeScript类型
│   ├── App.vue
│   └── main.ts
├── package.json
├── vite.config.ts
└── tsconfig.json
```

#### 4.2 核心页面设计

**登录/注册页面**：
- 用户名、密码输入
- 表单验证
- 记住登录状态

**问答页面（核心）**：
- 左侧：会话列表（新建、切换、删除会话）
- 中间：对话区域（消息展示、流式输出）
- 右侧：引用来源面板（相关知识库片段）

**知识库管理页面（管理员）**：
- 文档列表（搜索、分页、筛选）
- 文档上传（拖拽上传、进度显示）
- 文档详情（查看分块内容）
- 分类管理

#### 4.3 路由与权限控制
```typescript
const routes = [
  { path: '/login', component: Login },
  { path: '/register', component: Register },
  {
    path: '/',
    component: Layout,
    children: [
      { path: 'chat', component: Chat, meta: { requiresAuth: true } },
      { path: 'profile', component: Profile, meta: { requiresAuth: true } },
      {
        path: 'admin/knowledge',
        component: Knowledge,
        meta: { requiresAuth: true, requiresAdmin: true }
      }
    ]
  }
]
```

---

### 第五阶段：性能优化（第7周）

#### 5.1 缓存策略
- [ ] Redis缓存热门文档向量
- [ ] 对话历史缓存
- [ ] 用户会话缓存

#### 5.2 数据库优化
- [ ] 添加必要索引
- [ ] 查询优化
- [ ] 连接池配置

#### 5.3 API优化
- [ ] 请求压缩（GZip）
- [ ] 分页查询优化
- [ ] 异步处理耗时操作

#### 5.4 前端优化
- [ ] 路由懒加载
- [ ] 组件按需引入
- [ ] 静态资源缓存

---

### 第六阶段：测试与部署（第8周）

#### 6.1 测试
- [ ] 单元测试（pytest）
- [ ] API接口测试
- [ ] 前端功能测试
- [ ] RAG效果测试

#### 6.2 部署准备
```
# 本地启动方式
# 后端
cd backend
pip install -r requirements.txt
python run.py

# 前端
cd frontend
npm install
npm run dev
```

- [ ] 编写启动脚本
- [ ] 编写部署文档
- [ ] 环境变量配置说明

---

## 依赖安装

### 后端 requirements.txt
```
# Web框架
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# 数据库
sqlalchemy==2.0.25
aiosqlite==0.19.0
alembic==1.13.1

# 安全认证
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# LangChain核心
langchain==0.1.4
langchain-openai==0.0.5
langchain-community==0.0.16

# 向量数据库
pymilvus==2.3.6
langchain-milvus==0.0.5

# 文档处理
pypdf==3.17.1
docx2txt==0.8

# 工具库
pydantic==2.5.3
python-dotenv==1.0.0
aiofiles==23.2.1
httpx==0.26.0
```

### 前端 package.json
```json
{
  "name": "bankkb-frontend",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.15",
    "vue-router": "^4.2.5",
    "pinia": "^2.1.7",
    "axios": "^1.6.5",
    "element-plus": "^2.5.3",
    "@element-plus/icons-vue": "^2.3.1",
    "marked": "^11.1.1"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.0.11",
    "@vitejs/plugin-vue": "^5.0.3",
    "@types/node": "^20.11.5",
    "unplugin-auto-import": "^0.17.3",
    "unplugin-vue-components": "^0.26.0"
  }
}
```

---

## 环境变量配置

### .env 文件
```env
# 数据库
DATABASE_URL=sqlite+aiosqlite:///./bankkb.db

# JWT配置
JWT_SECRET_KEY=your-secret-key-change-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=120

# 阿里云百炼（通义千问）
DASHSCOPE_API_KEY=your-dashscope-api-key
LLM_MODEL=qwen-plus

# OpenAI Embedding
OPENAI_API_KEY=your-openai-api-key
EMBEDDING_MODEL=text-embedding-3-small

# Milvus
MILVUS_URI=./milvus_data.db
MILVUS_COLLECTION=bank_kb

# 文件上传
UPLOAD_DIR=./uploads
MAX_UPLOAD_SIZE=10485760
```

---

## 预计时间表

| 阶段 | 时间 | 主要产出 |
|------|------|----------|
| 第一阶段 | 第1周 | 项目框架、用户认证 |
| 第二阶段 | 第2周 | 知识库管理、文档处理 |
| 第三阶段 | 第3-4周 | RAG问答核心、流式响应 |
| 第四阶段 | 第5-6周 | 前端界面开发 |
| 第五阶段 | 第7周 | 性能优化 |
| 第六阶段 | 第8周 | 测试、部署文档 |

**总计：约8周**

---

## 确认事项

请确认以下配置信息，我将开始创建项目代码：

1. **阿里云百炼API Key**：您是否已经获取？
2. **OpenAI API Key**：用于Embedding，是否已有？
3. **开发目录**：是否在当前 `C:\workspace\LangChain-Agent` 目录下创建？
4. **数据库选择**：本地开发使用SQLite是否可以？（生产环境可切换PostgreSQL）

确认后我将开始编写代码！
